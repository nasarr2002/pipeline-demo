pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "Build du projet..."
            }
        }
        stage('Test') {
            steps {
                echo "Tests en cours..."
            }
        }
    }
}
